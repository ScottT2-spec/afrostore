// Stub — templates removed.

export async function importTemplateToSite(_siteId: string, _input: Record<string, unknown>) {
  return { template: null, siteTemplate: null, pages: [], themeConfig: {}, reused: false };
}
